# Copy to `.env` (git-ignored) and fill in. No key is needed for unit/contract/integration tests or
# the eval suite, which use a mock/scripted LLM and never touch the network.
#
# The provider is selected in config.yaml (`provider: anthropic | openai`); set the matching key here.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
