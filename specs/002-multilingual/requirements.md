# Requirements

This project is built with **GitHub Spec Kit**, which names the requirements document
[`spec.md`](spec.md) — user scenarios & acceptance tests, functional requirements (FR-…),
and measurable success criteria (SC-…).

This pointer exists so the assessment's expected `requirements.md` filename resolves directly.
**The authoritative requirements are in [`spec.md`](spec.md).**
